volumes:
  sockets:
    driver_opts:
      type: tmpfs
      device: tmpfs

networks:
  rinha:
    driver: bridge

services:
  lb:
    build:
      context: .
      dockerfile: Dockerfile.lb
    container_name: rinha-lb
    ports:
      - "9999:9999"
    volumes:
      - sockets:/sockets
    depends_on:
      - api1
      - api2
    networks:
      - rinha
    cpuset: "2,3"
    deploy:
      resources:
        limits:
          cpus: "0.20"
          memory: "30MB"

  api1:
    #image: hisraelbraga/hisrael-rinha-rust-2026:initial
    build:
      context: .
    environment:
      - PORT=8080
      - INSTANCE_ID=1
    volumes:
      - sockets:/tmp/sockets
    networks:
      - rinha
    cpuset: "0"
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "160MB"

  api2:
    #image: hisraelbraga/hisrael-rinha-rust-2026:initial
    build:
      context: .
    environment:
      - PORT=8080
      - INSTANCE_ID=2
    volumes:
      - sockets:/tmp/sockets
    networks:
      - rinha
    cpuset: "1"
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "160MB"
